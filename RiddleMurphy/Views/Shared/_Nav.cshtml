
<nav class="navbar navbar-expand-sm navbar-dark bg-black fixed-top">
    <div class="container-fluid">

        <a class="navbar-brand" href="/">DISCOVER</a>

        @if (User.IsInRole("U"))
        {
            <a class="navbar-brand" href="/Main/Home">HOME</a>
        }

        <button onclick="ShowProfileChilds()" class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto">

                @if (User.IsInRole("U"))
                {
                    <li id="nav-item1" class="nav-item">
                        <a class="nav-link" href="/User">Profile</a>
                    </li>
                    <li id="nav-item2" class="nav-item">
                        <a class="nav-link" href="/User/Search">Search</a>
                    </li>
                }

                @if (User.IsInRole("A"))
                {
                    <li id="nav-item3" class="nav-item">
                        <a class="nav-link" href="/Mod">Moderating</a>
                    </li>
                }

                @if (User.IsInRole("A"))
                {
                    <li id="nav-item3" class="nav-item">
                        <a class="nav-link" href="/Mod/TodaysRiddle">Set Todays Riddle</a>
                    </li>
                }

                @if (@HttpContext.Current.User.Identity.IsAuthenticated)
                {
                    <li id="nav-item4" class="text-end">
                        <a class="nav-link text-danger" href="/Security/LogOut">Log Out</a>
                    </li>
                }

            </ul>

            @if (!@HttpContext.Current.User.Identity.IsAuthenticated)
            {
                <div class="col-md-3 text-end mt-1">
                    <a id="nav-item5" href="/Security/Login" type="button" class="btn btn-outline-primary me-2">Login</a>
                    <a id="nav-item6" href="/Main/SignUp" type="button" class="btn btn-primary">SignUp</a>
                </div>
            }

        </div>
    </div>
</nav>
